# Terraform Backend Configuration for Config Layer - Production Environment
terraform {
  required_version = "~> 1.13.0"
  backend "s3" {
    bucket         = "salesforce-app-terraform-state"
    key            = "config/prod/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    versioning     = true
  }
}