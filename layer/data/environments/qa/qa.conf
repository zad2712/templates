# Terraform Backend Configuration for Data Layer - QA Environment
terraform {
  required_version = "~> 1.13.0"
  backend "s3" {
    bucket         = "salesforce-app-terraform-state"
    key            = "data/qa/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    versioning     = true
  }
}